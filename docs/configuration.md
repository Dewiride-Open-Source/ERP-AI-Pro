# Configuration register

Every configuration key, feature flag and secret **name** in the system. Values never appear here. Keys follow `Erp:<Domain>:<Module>:<Setting>`; feature flags follow `Erp.Modules.<Domain>.<Module>[.<Capability>]`; secrets follow `Erp--<Domain>--<Module>--<Name>` (see ADR-0005).

## Bootstrap environment variables

| Variable | Used by | Purpose |
|---|---|---|
| `ASPNETCORE_ENVIRONMENT` | API | `Development` or `Production` framework behaviour |
| `DOTNET_ENVIRONMENT` | migrator | `Development` or `Production` framework behaviour of the migrator, a generic host that ignores `ASPNETCORE_ENVIRONMENT`; `Development` locally (launch profiles, `compose.override.yaml`, `e2e.yml`), `Production` in the production stack |
| `ASPNETCORE_HTTP_PORTS` | API container | listening port (8080 in containers) |
| `ERP_ENVIRONMENT` | API | App Configuration label, `local-dev` or `production`; required, and validated, whenever `APPCONFIG_ENDPOINT` is set (`local-dev` from `launchSettings.json` on developer machines, `production` from `compose.production.yaml`) |
| `APPCONFIG_ENDPOINT` | API | Azure App Configuration endpoint (`https://<store>.azconfig.io`); when set the API loads the store and its Key Vault references, when empty a Development host runs on `appsettings.json` plus user secrets and a Production host refuses to start; developers keep it in `dotnet user-secrets`, never in a committed file |
| `ERP_CONFIGURATION_SOURCE` | integration tests, `dotnet ef` | bootstrap value with two settings, never set by hand: `InMemory`, set by `ErpApiFactory` through `UseSetting` so every test host, Development or Production, runs on in-memory configuration without Azure; `LocalDevelopment`, set by the host itself (`ErpHostComposition.AddErpPlatform`) when `EF.IsDesignTime` is true, before `AddErpConfiguration` runs, so `dotnet ef` never contacts the store and reads `appsettings.json`, user secrets and the environment in any `ASPNETCORE_ENVIRONMENT` |
| `ERP_TEST_SQL_CONNECTION` | integration tests (`SqlTestDatabase`) | server-level connection string with rights to create databases and no `Database=` part; the assembly fixture creates `ErpAiProTest_<yyyyMMddHHmmss>_<8 hex>` from it per test process, migrates every catalogue context and drops it at the end, and fails with a message naming the variable when it is missing; on the owner's machine the Windows sign-in string `Server=localhost;Integrated Security=True;Encrypt=True;TrustServerCertificate=True` as a user environment variable, in CI the `server-connection-string` output of `.github/actions/start-sql-server` |
| `ERP_TEST_BLOB_EMULATOR_HOST` | integration tests (`BlobTestContainer`), `scripts/verify/verify.ts`, `scripts/api-client/generate.ts` | host of the Azurite blob emulator on port 10000 that every test project declaring the `BlobTestContainer` assembly fixture uses; the fixture creates the container `erptest-<yyyyMMddHHmmss>-<8 hex>` on it per test process, deletes `erptest-*` containers older than 24 hours and its own at the end, and fails with a message naming the variable when it is missing; `ErpApiFactory` passes the host to every test host as `Erp:Platform:Attachments:EmulatorHost`. `127.0.0.1` on the owner's machine (a user environment variable, with the `erp-azurite` container of `docs/guides/local-development.md` running), in CI the `emulator-host` output of `.github/actions/start-azurite`; `verify.ts` (unless `--skip-backend`) and `generate.ts` (unless `--skip-snapshot`) stop early when it or `ERP_TEST_SQL_CONNECTION` is missing |
| `ERP_OPENAPI_SNAPSHOT` | `OpenApiSnapshotTests`, `scripts/api-client/generate.ts` | `update` makes the snapshot test rewrite `docs/openapi/erp.json` from the running test host instead of failing on a difference; unset everywhere else, never set in CI |
| `AZURE_TOKEN_CREDENTIALS` | API | mandatory selector for `DefaultAzureCredential`: `AzureCliCredential` on developer machines (from `launchSettings.json`; only the `az login` session is used, never a Visual Studio or VS Code sign-in) and `EnvironmentCredential` in the api container (from `compose.production.yaml`); outside `Development` the credential factory rejects any other value at startup. The resulting `TokenCredential` is registered once in DI by `AddErpConfiguration` and also signs the API in to Blob Storage (`Erp:Platform:Attachments:BlobServiceUri`); without the store it is created only when a storage client needs it, so emulator mode and tests need none. The gated API that Playwright starts sets `AzureCliCredential` itself, because it runs without the store but against the development storage account |
| `AZURE_TENANT_ID`, `AZURE_CLIENT_ID` | API container | tenant and application (client) id of the runtime service principal the container signs in as; both required in Production |
| `AZURE_CLIENT_ID` (migrator) | migrator container (production) | application (client) id of the migrator service principal, from `MIGRATOR_AZURE_CLIENT_ID`; the migrator signs in with its own certificate `/run/secrets/erp-migrator-client.pem` (ADR-0021) |
| `AZURE_APP_CONFIGURATION_FM_SCHEMA_COMPATIBILITY_DISABLED` | API (set by the API itself) | `true`, set by `AddErpConfiguration` in the process environment before the store is connected so the provider emits every feature flag in the Microsoft schema (ADR-0012); never set by hand |
| `AZURE_CLIENT_CERTIFICATE_PATH` | API container | path of the runtime certificate secret file, `/run/secrets/erp-runtime-client.pem`, which the credential factory opens for reading at startup so an unreadable file fails with a message naming the fix; must name an existing `.pem` or `.pfx` file in Production, and `AZURE_CLIENT_SECRET` must not be set alongside it |
| `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_PROTOCOL` | API, web | OTLP exporter; telemetry export is off when the endpoint is unset. The API speaks gRPC. The web app validates both: the endpoint is an `http` or `https` base URL (a path prefix is allowed, credentials are not, trailing slashes are stripped) and the protocol is `http/protobuf` (default) or `http/json`; the web app builds its trace exporter from the validated values (`<endpoint>/v1/traces`) and consults no other `OTEL_EXPORTER_*` variable |
| `OTEL_DOTNET_EXPERIMENTAL_SQLCLIENT_ENABLE_TRACE_DB_QUERY_PARAMETERS` | API | never set: it would copy every SQL parameter value (PAN, bank numbers, salaries) into trace attributes; without it SQL spans carry only the parameterised command text (`docs/guides/observability.md`, ADR-0020) |
| `OTEL_DOTNET_EXPERIMENTAL_ASPNETCORE_DISABLE_URL_QUERY_REDACTION` | API | never set: the ASP.NET Core instrumentation replaces every query value in the server span's `url.query` with `Redacted` by default, which keeps attachment download tokens (`?link=`) out of every span; the variable would export them (`docs/guides/observability.md`, ADR-0022) |
| `DOTNET_SYSTEM_NET_HTTP_DISABLEURIREDACTION` (the `System.Net.Http.DisableUriRedaction` switch) | API | never set: on .NET 9 and later the runtime redacts the `url.full` of every outgoing `HttpClient` span (the whole query becomes `*`, user info and fragment are dropped) and the redaction of `IHttpClientFactory` logs; the switch turns both off. `OTEL_DOTNET_EXPERIMENTAL_HTTPCLIENT_DISABLE_URL_QUERY_REDACTION` has no effect on this runtime, because `OpenTelemetry.Instrumentation.Http` leaves the runtime's span attributes unchanged |
| `OTEL_SERVICE_NAME` | web | service name for `@vercel/otel`; optional, default `erp-ai-pro-web`, 1–120 characters |
| `NODE_ENV` | web | `development`, `production` or `test` (default `development`); `next start` and the standalone server set `production`, which makes `API_INTERNAL_URL` mandatory |
| `API_INTERNAL_URL` | web | origin of the API (`scheme://host[:port]`, no path, query, fragment or credentials; normalised to the origin, so `http://api:8080/` becomes `http://api:8080`) for server-side API calls (`shared/api/client.ts`, the base URL of the generated client, whose routes already start with `/api`) and the runtime `/api/*` and `/openapi/*` rewrite in `proxy.ts`; single-label hosts such as `http://api:8080` accepted; required when `NODE_ENV=production`, default `http://localhost:5080` in development. Every web variable in this table is validated by `src/shared/config/env.schema.ts`: `next start` and the standalone server exit with a message naming the offending variable (`scripts/checks/web-startup-guard.ts` proves it after every build), `next build` succeeds without it |
| `NEXT_PUBLIC_APP_NAME` | web (build time) | display name in the shell and the browser title, 1–60 characters, default `ERP-AI-Pro`; read by `next.config.ts` when `next build` runs and inlined into the bundle, so at runtime the variable has no effect — the image takes it as a build argument (table below) |
| `E2E_BASE_URL`, `E2E_API_BASE_URL` | Playwright | target origins; when `E2E_BASE_URL` is unset Playwright starts the API and the web app itself |
| `E2E_OFFLINE_BASE_URL` | Playwright | origin of the web instance whose API is unreachable (port 3001 when Playwright starts it), for the API-unavailable states |
| `E2E_GATED_BASE_URL`, `E2E_GATED_API_BASE_URL` | Playwright | origins of the web and API pair started with `Erp.Modules.Platform.SystemInfo` and `Erp.Modules.Platform.Attachments` disabled (`gatedFeatureFlags` in `frontend/e2e/fixtures/targets.ts`; ports 3002 and 5081 when Playwright starts them), for the disabled-module states |

### Compose interpolation (`infra/compose/.env`)

| Variable | Purpose |
|---|---|
| `IMAGE_TAG` | image tag pulled from ghcr.io (`local` for locally built images) |
| `COMPOSE_NETWORK_CIDR` | subnet of the compose network; the API trusts forwarded headers only from this range |
| `APPCONFIG_ENDPOINT` | passed to the API as `APPCONFIG_ENDPOINT`; required by the production stack, which refuses to start without it; left empty on a developer machine because the local stack cannot sign in to Azure and runs on `appsettings.json` (`dotnet run` uses user secrets instead) |
| `AZURE_TENANT_ID`, `AZURE_CLIENT_ID` | production stack only; passed to the api container as the runtime service principal's tenant and application (client) id (ids, never secrets; the certificate is the compose secret `erp-runtime-client.pem` from `infra/compose/secrets/`) |
| `MIGRATOR_AZURE_CLIENT_ID` | production stack only; passed to the migrator container as `AZURE_CLIENT_ID` — the migrator service principal, whose contained database user holds the schema rights (an id, not a secret) |
| `OTEL_EXPORTER_OTLP_ENDPOINT`, `WEB_OTEL_EXPORTER_OTLP_ENDPOINT` | local Aspire dashboard endpoints for the API (gRPC) and the web app (OTLP/HTTP) |

### Compose secret files (`infra/compose/secrets/`, git-ignored)

A compose secret is a file, never an `.env` variable; the api container reads the directory as `/run/secrets` through the key-per-file provider, which turns `__` into `:`.

| File | Stack | Purpose |
|---|---|---|
| `Erp__Platform__Database__ConnectionString` | local (`compose.override.yaml`), the `docker-build` smoke | lands on `Erp:Platform:Database:ConnectionString`; on the owner's machine `Server=host.docker.internal,1433;Database=ErpAiPro;User ID=<login>;Password=<password>;Encrypt=True;TrustServerCertificate=True`, a SQL login because a container cannot use Windows sign-in; the compose smoke script refuses to start without the file and names it |
| `Erp__Platform__Database__MigratorConnectionString` | local (`compose.override.yaml`, migrator service only), the `docker-build` smoke | lands on `Erp:Platform:Database:MigratorConnectionString`; locally the `erp_local_migrator` SQL login (`db_ddladmin`, `db_datareader`, `db_datawriter` on `ErpAiPro`) at `host.docker.internal,1433`; in CI the run-time `sa` connection |
| `Erp__Platform__Attachments__EncryptionKey` | local (`compose.override.yaml`, api service only), the `docker-build` smoke | lands on `Erp:Platform:Attachments:EncryptionKey`: one line `<key id>:<32 random bytes in base64>`, generated on the machine without being displayed (`printf 'local:%s' "$(openssl rand -base64 32)"`, `infra/compose/.env.example`); it protects only the attachments the stack keeps in its `azurite` service and never enters a vault; in CI generated per run as `ci:<…>` and masked; the compose smoke script refuses to start without the file and names it. The migrator never receives it |
| `erp-runtime-client.pem` | production (`compose.production.yaml`) | the runtime service principal's certificate, hidden from the key-per-file provider and read only through `AZURE_CLIENT_CERTIFICATE_PATH` |
| `erp-migrator-client.pem` | production (`compose.production.yaml`) | the migrator service principal's certificate, opened at startup like the runtime certificate; created by the first-deployment database provisioning |

### Image build arguments (`infra/docker/web.Dockerfile`)

| Argument | Default | Purpose |
|---|---|---|
| `NEXT_PUBLIC_APP_NAME` | `ERP-AI-Pro` | inlined display name; pass `--build-arg NEXT_PUBLIC_APP_NAME=...` to `docker build` (or `build.args` in a compose override) to rename the product in a custom image |

## Configuration keys (`appsettings.json` / App Configuration)

| Key | Type | Default | Purpose |
|---|---|---|---|
| `Erp:Platform:Host:ApplicationName` | string | `ERP-AI-Pro` | name reported by the system-info endpoint and, from the `backend-platform` phase, the Data Protection application name and telemetry service name; seeded unlabelled from `infra/appconfig/defaults.json` and overridden to `ERP-AI-Pro (local-dev)` under the `local-dev` label, which makes the environment visible on the system page and doubles as the label-precedence proof of `verify.sh --labels` |
| `Erp:Platform:Host:AllowedHosts` | string | `*` | `;`-separated host names the API answers (case-insensitive, port ignored, `*.` subdomain wildcards, entries trimmed); any other `Host` answers 400 `request.host-not-allowed` from `AllowedHostsMiddleware` (the framework's own host filter stays permissive because the root `AllowedHosts` key is never set). Seeded unlabelled from `infra/appconfig/defaults.json`; the `first-deployment` phase narrows it under the `production` label to `<public host name>;api;127.0.0.1`, the three names the API is called by (the edge proxy passing the browser's `Host` for `/api/*`, the web container's server-side calls and the container health probe) |
| `Erp:Platform:Host:KnownNetworks` | string[] | `[]` | networks in CIDR notation, each with its exact network address, whose peers may send `X-Forwarded-For`/`X-Forwarded-Proto` (the compose network in production); an empty list trusts no proxy, and an invalid or host-bit entry such as `172.28.0.5/16` fails startup naming it; host-local, set through `Erp__Platform__Host__KnownNetworks__0` in the compose file and never seeded in the store. `ASPNETCORE_FORWARDEDHEADERS_ENABLED` is never set: it would trust every peer |
| `Erp:Platform:Host:MaxRequestBodyBytes` | long | `1048576` | largest request body Kestrel accepts (1 KiB to 1 GiB); a larger body answers 413 `request.too-large`; seeded unlabelled from `infra/appconfig/defaults.json`; an endpoint that must accept more raises its own limit through `IRequestSizeLimitMetadata`, which the attachments upload does (`Erp:Platform:Attachments:MaxSizeBytes` plus 64 KiB) |
| `Erp:Platform:Host:RequestTimeout` | TimeSpan | `00:00:30` | default time a request may run (1 second to 10 minutes) before the request-timeouts middleware cancels it and the API answers 504 `request.timeout`; the web app's rewrite proxy waits `experimental.proxyTimeout` (660 000 ms in `frontend/apps/web/next.config.ts`), which must stay above the 10-minute upper bound so the API's answer always arrives first; seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:RateLimiting:Enabled` | bool | `true` | switches the global rate limiter on; `false` leaves every request unlimited except the attachments upload, whose own concurrency policy (`Erp:Platform:Attachments:MaxConcurrentUploads`) does not depend on this switch; read once at startup, like every limit below; `RateLimitingOptions`, seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:RateLimiting:AnonymousPermitLimit` | int | `600` | requests an anonymous caller may make per `AnonymousWindow`, counted per client address (the forwarded address from a trusted proxy; an IPv6 client by its /64 prefix); over it the API answers 429 `rate-limit.exceeded` with `Retry-After`; 1 to 1 000 000; seeded unlabelled from `infra/appconfig/defaults.json`, and `6000` under `local-dev` from `infra/appconfig/local-dev.json`, because on a developer machine the browser and the web server's own calls all reach the API from the loopback address, so a Playwright run across every browser project shares one partition (CI runs store-less, one project per job, under the default `600`) |
| `Erp:Platform:RateLimiting:AnonymousWindow` | TimeSpan | `00:01:00` | fixed window of the anonymous limit (1 second to 1 hour); seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:RateLimiting:ActorPermitLimit` | int | `600` | requests a signed-in actor may make per `ActorWindow`, counted per actor across every address (never per address, so colleagues behind one office connection keep their own allowance); 1 to 1 000 000; seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:RateLimiting:ActorWindow` | TimeSpan | `00:01:00` | sliding window of the actor limit (1 second to 1 hour); seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:RateLimiting:ActorSegmentsPerWindow` | int | `6` | segments the actor window slides by (1 to 60; six gives 10-second steps for a one-minute window); seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Caching:DefaultExpiration` | TimeSpan | `00:05:00` | default lifetime of a `HybridCache` entry, in process and in any second-level cache (1 second to 1 day); `CachingOptions`, seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Caching:MaximumPayloadBytes` | long | `1048576` | largest value `HybridCache` stores (1 KiB to 64 MiB); a larger value is computed but not cached; seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Configuration:RefreshInterval` | TimeSpan | `00:30:00` | how often the API checks the labelled `Erp:Sentinel` and the feature flags (between 1 second and 1 day); bootstrap-only: `appsettings.json`, environment or user secrets; read before the store is connected, not refreshable, never seeded in the store |
| `Erp:Platform:Configuration:SecretRefreshInterval` | TimeSpan | `01:00:00` | how long a resolved Key Vault reference is cached before the secret is read again (between 1 minute and 7 days); bootstrap-only: `appsettings.json`, environment or user secrets; read before the store is connected, not refreshable, never seeded in the store |
| `Erp:Platform:Configuration:StartupTimeout` | TimeSpan | `00:01:00` | how long the first load from the store may take before startup fails (between 1 second and 10 minutes); bootstrap-only: `appsettings.json`, environment or user secrets; read before the store is connected, not refreshable, never seeded in the store |
| `Erp:Platform:Database:ConnectionString` | Key Vault reference | — | connection string of the one ERP database, required at run time and optional at EF design time; labelled `local-dev`, resolving to `Erp--Platform--Database--ConnectionString` in the local-dev vault (`infra/appconfig/key-vault-references.json` entry with `"labels": ["local-dev"]`); the `production` reference is added by the first-deployment phase once the production secret exists. Read per process: `dotnet run` with the store through the reference; `dotnet ef` and a store-less Development run from `dotnet user-secrets` of `Hosts/Api/Dewiride.Erp.Host.Api`; CI from the environment variable `Erp__Platform__Database__ConnectionString`; containers from the key-per-file secret `/run/secrets/Erp__Platform__Database__ConnectionString`; tests from `SqlTestDatabase` through `ErpApiFactory`, which overrides every other source |
| `Erp:Platform:Database:Provider` | `DatabaseProvider` (`SqlServer` or `AzureSql`) | `SqlServer` | `SqlServer` selects `UseSqlServer` with compatibility level 170, `AzureSql` selects `UseAzureSql`, both with the same history table, timeout and retry settings; seeded unlabelled as `SqlServer` from `infra/appconfig/defaults.json` and overridden to `AzureSql` under the `production` label from `infra/appconfig/production.json` |
| `Erp:Platform:Database:CommandTimeout` | TimeSpan | `00:00:30` | command timeout on every module context (between 1 second and 10 minutes); seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Database:MaxRetryCount` | int | `5` | `EnableRetryOnFailure` retry count (0 to 20); seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Database:MaxRetryDelay` | TimeSpan | `00:00:10` | `EnableRetryOnFailure` maximum delay between retries (between 1 second and 2 minutes); seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Database:MigratorConnectionString` | string (compose secret file locally; Key Vault reference under `production`) | — | connection the migrator uses instead of `ConnectionString` when set (`MigratorDatabaseOptions`, `MigratorConnectionStringSetup`): the deployment identity with schema rights, while the API keeps data rights only; unset for host processes, which fall back to the Windows sign-in; the production reference is added by the first-deployment database provisioning (ADR-0021) |
| `Erp:Platform:Idempotency:RetentionPeriod` | TimeSpan | `1.00:00:00` | how long an `Idempotency-Key` stays recognised after its first use (between 5 minutes and 30 days); a repeat inside the window replays the stored response, after it the key is free again; `IdempotencyOptions`, seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Idempotency:MaxStoredResponseBytes` | int | `1048576` | largest response body stored for replay (1 KiB to 16 MiB); a larger response is still sent to the first caller but a repeat answers 409 `idempotency.replay-unavailable`; seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Idempotency:CleanupInterval` | TimeSpan | `01:00:00` | how often `IdempotencyCleanupService` deletes expired keys from `platform_idempotency.IdempotencyKeys` (between 1 minute and 1 day); seeded unlabelled from `infra/appconfig/defaults.json` |
| `Erp:Platform:Attachments:BlobServiceUri` | string | — | blob endpoint of the environment's Entra-only storage account, `https://<account>.blob.core.windows.net/` (absolute `https`, path `/`, no query); exactly one of this key and `EmulatorHost` must be set, a blank value counting as unset. Labelled `local-dev`, written by `scripts/azure/provision.sh` from the deployment output (`https://sterpaiprodev.blob.core.windows.net/`), which bumps the `local-dev` sentinel when the value changed; the seed files must not carry it (`seed-files.ts` refuses it as script-owned); the `production` value is written by `first-deployment-production-attachments-storage`. A store-less `dotnet run` and the gated API Playwright starts read it from `dotnet user-secrets` of `Hosts/Api/Dewiride.Erp.Host.Api` (the value `provision.sh` prints as `ATTACHMENTS_BLOB_ENDPOINT_DEV`); tests blank it through `ErpApiFactory`; CI and the local containers leave it unset. Read when the storage clients are built, so a change takes effect at the next start; the API signs in with the `TokenCredential` of `AddErpConfiguration` and holds Storage Blob Data Contributor on the container only |
| `Erp:Platform:Attachments:EmulatorHost` | string | — | host of an Azurite blob emulator on port 10000, used instead of a storage account through the emulator's well-known development account (no key is configured anywhere): `127.0.0.1`, `localhost`, `host.docker.internal` or a single-label compose service name (`^[a-z][a-z0-9-]{0,62}$`). Host-local and never seeded (`seed-files.ts` refuses it): `azurite` from `compose.override.yaml` (`Erp__Platform__Attachments__EmulatorHost`), `127.0.0.1` from `e2e.yml` for both API starts, `BlobTestContainer.Current.EmulatorHost` through `ErpApiFactory` in tests; never in user secrets or the store, because setting it beside `BlobServiceUri` fails startup. In this mode `AttachmentStorageInitializer` creates the container at startup; read once, at the next start |
| `Erp:Platform:Attachments:ContainerName` | string | `attachments` | container that holds the envelope blobs; 3 to 63 lowercase letters, digits and single hyphens, starting and ending with a letter or digit. Not seeded: the code default matches the container `scripts/azure/bicep/main.bicep` creates (`attachmentsContainerName`); tests set the per-process `erptest-…` container through `ErpApiFactory`; read once, at the next start |
| `Erp:Platform:Attachments:MaxSizeBytes` | long | `26214400` | largest file an upload may carry (1 byte to 104 857 600, `AttachmentsOptions.MaxSizeBytesCeiling`, 100 MiB); a larger file answers 413 `attachment.too-large`, and a request body over this plus 64 KiB of multipart framing answers 413 `request.too-large`; reported by `GET /api/platform/attachments/policy` so the web app refuses a larger file before sending it; seeded unlabelled from `infra/appconfig/defaults.json`; applies on refresh |
| `Erp:Platform:Attachments:AllowedContentTypes` | string | the nine checked types | `;`-separated media types an upload may declare; each must be one the API can check against the file's content and name (`application/pdf`, `image/png`, `image/jpeg`, `image/gif`, `image/webp`, `text/plain`, `text/csv`, the `.xlsx` and `.docx` Open XML types), otherwise startup fails naming the entry; another declared type answers 415 `attachment.unsupported-type`, and an allowed type whose file name does not end in one of its extensions answers 415 `attachment.extension-mismatch`; seeded unlabelled from `infra/appconfig/defaults.json` with all nine; applies on refresh |
| `Erp:Platform:Attachments:DownloadLinkLifetime` | TimeSpan | `00:05:00` | how long a download link stays valid after `POST /api/platform/attachments/{id}/download-links` (30 seconds to 1 hour); seeded unlabelled from `infra/appconfig/defaults.json`; applies on refresh to new links |
| `Erp:Platform:Attachments:TransferTimeout` | TimeSpan | `00:10:00` | request timeout of the upload and the download, the named policy `platform.attachments.transfer` (30 seconds to 10 minutes; the web app's rewrite gives up after 660 seconds); seeded unlabelled from `infra/appconfig/defaults.json`; takes effect at the next start |
| `Erp:Platform:Attachments:UploadReservationLifetime` | TimeSpan | `01:00:00` | age after which `AttachmentsSweeper` treats an upload reservation as abandoned and removes its blob (1 minute to 6 days, and at least twice `TransferTimeout`, so an upload in progress is never swept; below the seven days after which Blob Storage discards uncommitted blocks itself); not seeded, the code default applies; takes effect at the next start |
| `Erp:Platform:Attachments:SweepInterval` | TimeSpan | `00:15:00` | how often `AttachmentsSweeper` runs (1 minute to 1 day; the first run one interval after start): each run removes the blobs of up to 100 abandoned upload reservations and deletes the download links that expired more than an hour ago without a redemption; not seeded; takes effect at the next start |
| `Erp:Platform:Attachments:MaxConcurrentUploads` | int | `16` | uploads the API process accepts at once (1 to 1024), the permit limit of the rate-limiter policy `platform.attachments.uploads` (one partition for the process, no queue), because every upload in flight holds a 4 MiB staging block for its whole transfer; the next upload answers 429 `rate-limit.exceeded` with `Retry-After`, whether or not `Erp:Platform:RateLimiting:Enabled` is set; not seeded, the code default applies; takes effect at the next start |
| `Erp:Platform:Attachments:EncryptionKey` | Key Vault reference | — | the key-encryption key of the attachments envelope, `<key id>:<32 random bytes in base64>` (key id 1 to 32 of `A-Z a-z 0-9 . _ -`); required when the API starts, never needed by the migrator. Labelled `local-dev`, resolving to `Erp--Platform--Attachments--EncryptionKey` in the local-dev vault (`infra/appconfig/key-vault-references.json` entry with `"labels": ["local-dev"]`); the `production` reference is added by `first-deployment-production-attachments-storage` once the production secret exists. Read per process: `dotnet run` with the store through the reference; a store-less run from `dotnet user-secrets` (a copy of the development vault's key, written without display by the blocks of `docs/operations/runbooks/secrets.md` section 5f and `docs/guides/local-development.md`, so both runs read the same blobs in the shared development container); tests from `ErpApiFactory` (one generated `test:` key per test process); `e2e.yml` from the environment variable `Erp__Platform__Attachments__EncryptionKey`, generated and masked per run; the local containers from the key-per-file secret `/run/secrets/Erp__Platform__Attachments__EncryptionKey`. `KeyRing` picks up a refreshed value without a restart; rotation is `docs/operations/runbooks/secrets.md` section 5f |
| `Erp:Platform:Attachments:RetiredEncryptionKeys` | Key Vault reference | — | `;`-separated keys of the same shape that the API only decrypts with, so attachments wrapped before a rotation stay readable; the same key may also be the current `EncryptionKey` while a rotation is in flight, but one id with two different keys fails validation. Resolves to `Erp--Platform--Attachments--RetiredEncryptionKeys`; its `key-vault-references.json` entry is added at the first rotation, per label whose vault holds the secret; unset until then |
| `Erp:Sentinel` | string | — | labelled `local-dev` and `production`; the value is the UTC timestamp of the last bump, and bumping it triggers a full configuration refresh in the API at the next check |
| `Erp:Platform:Identity:TenantId` | string | — | labelled `local-dev` and `production`; the Entra tenant id, written by `scripts/azure/entra.sh` (sub-phase `azure-configuration-entra-app-registration-scripts`) and bound by the `authentication` phase |
| `Erp:Platform:Identity:ClientId` | string | — | labelled `local-dev` and `production`; the application (client) id of that environment's sign-in registration, written by `scripts/azure/entra.sh` and bound by the `authentication` phase |
| `Erp:Platform:Identity:Instance` | string | `https://login.microsoftonline.com/` | Entra authority the sign-in redirects to; seeded unlabelled from `infra/appconfig/defaults.json` and bound by the `authentication` phase |
| `Erp:Platform:Identity:ClientCertificate` | Key Vault reference | — | labelled `local-dev` and `production`; resolves to `Erp--Platform--Identity--ClientCertificate` in that environment's vault (URI composed by `scripts/azure/seed.sh` from `infra/appconfig/key-vault-references.json`), the PKCS#12 sign-in certificate the `authentication` phase presents to Entra |

### Attachment settings by process

Where each `Erp:Platform:Attachments` value comes from ([attachments](architecture/attachments.md)); "default" is the `AttachmentsOptions` default, "seeded" is `infra/appconfig/defaults.json` (unlabelled):

| Key | `dotnet run` with the store | store-less `dotnet run`, gated Playwright API | tests (`ErpApiFactory`) | `e2e.yml` | local containers | production |
|---|---|---|---|---|---|---|
| `BlobServiceUri` | `local-dev` label, written by `provision.sh` | user secrets | blanked | unset | unset | `production` label, written by `first-deployment-production-attachments-storage` |
| `EmulatorHost` | unset | unset | `BlobTestContainer` host | `127.0.0.1` | `azurite` | unset |
| `ContainerName` | default | default | per-process `erptest-…` | default | default | default |
| `EncryptionKey` | Key Vault reference (`local-dev`) | user secrets | generated per test process | generated per run, masked | compose secret file | Key Vault reference (`production`), from `first-deployment-production-attachments-storage` |
| `RetiredEncryptionKeys` | Key Vault reference after the first rotation | user secrets when needed | unset | unset | unset | Key Vault reference after the first rotation |
| `MaxSizeBytes`, `AllowedContentTypes`, `DownloadLinkLifetime`, `TransferTimeout` | seeded | defaults | defaults | defaults | defaults | seeded |
| `UploadReservationLifetime`, `SweepInterval`, `MaxConcurrentUploads` | default | default | default | default | default | default |

The migrator uses none of them and starts without any of them (`MigratorApplicationTests`).

## Feature flags

| Flag | Purpose |
|---|---|
| `Erp.Modules.Platform.SystemInfo` | module flag of the system-info module; seeded from `infra/appconfig/feature-flags.json` as enabled under `local-dev` and `production` |
| `Erp.Modules.Platform.Attachments` | module flag of the attachments module: gates the seven routes under `/api/platform/attachments` and the web route `/platform/attachments`; seeded from `infra/appconfig/feature-flags.json` as enabled under `local-dev` and `production`. It gates only the HTTP surface: the `files` schema, the storage clients, the sweeper and `IAttachmentService` for other modules stay registered while it is off |

Evaluation (ADR-0012): the `FeatureCatalog` derives one flag per registered module and one per declared `ModuleCapability`; a module flag that no configuration source defines evaluates enabled, a capability flag follows its declared default, and a definition from a later configuration source overrides an earlier one by id (App Configuration beats environment variables, which beat `appsettings.json`; the API runs the store with `AZURE_APP_CONFIGURATION_FM_SCHEMA_COMPATIBILITY_DISABLED=true`, set by `AddErpConfiguration` itself, so store flags use the same Microsoft schema as every other source and that order holds). Any source may define a flag with the Microsoft schema — as environment variables `feature_management__feature_flags__<n>__id=Erp.Modules.<Domain>.<Module>` and `feature_management__feature_flags__<n>__enabled=true|false`, as the same keys in `appsettings.json`, or as App Configuration feature flags; production values come from App Configuration. A disabled module flag makes every route of the module answer `404 application/problem+json` with `code: feature.disabled`, hides the module in the web shell and turns its web segment into the in-shell not-found page. `GET /api/platform/features` reports every catalog flag with its evaluated state.

## Secrets (Key Vault)

| Secret | Environment | Purpose |
|---|---|---|
| `Erp--Platform--Database--ConnectionString` | local-dev; production from the first-deployment phase | connection string of the ERP database for host processes, referenced by `Erp:Platform:Database:ConnectionString` and written by `scripts/azure/seed.sh` only under the labels the reference lists; local-dev: the Windows sign-in string `Server=localhost;Database=ErpAiPro;Integrated Security=True;Encrypt=True;TrustServerCertificate=True` (no credential inside), created with `az keyvault secret set --file` in the session that introduces the key, with the owner's consent; production: the Azure SQL string with `Authentication="Active Directory Default";Encrypt=True;TrustServerCertificate=False`, created in the first-deployment phase |
| `Erp--Platform--Attachments--EncryptionKey` | local-dev (created 2026-09-26 in `kv-erp-ai-pro-dev`); production from `first-deployment-production-attachments-storage` | the attachments key-encryption key, `<key id>:<32 random bytes in base64>`, referenced by `Erp:Platform:Attachments:EncryptionKey` and written by `scripts/azure/seed.sh` only under the labels the reference lists; generated inside the command that stores it and never displayed, a separate value per vault (the production key is never a copy of the development one); losing every copy makes the attachments it wrapped unreadable. Creation and rotation: `docs/operations/runbooks/secrets.md` section 5f |
| `Erp--Platform--Attachments--RetiredEncryptionKeys` | created at the first rotation, per vault | `;`-separated retired attachments keys the API only decrypts with, referenced by `Erp:Platform:Attachments:RetiredEncryptionKeys`; an entry is removed only when no `files.StoredContents.KeyId` names its id |
| `Erp--Platform--Identity--ClientCertificate` | local-dev, production | PKCS#12 certificate with private key of that environment's sign-in registration, created by `scripts/azure/entra.sh`; referenced by `Erp:Platform:Identity:ClientCertificate`, written by `scripts/azure/seed.sh` only after the secret exists |
| `Erp--Platform--Identity--RuntimeClientCertificate` | production | PEM certificate with private key of the runtime service principal, created by `scripts/azure/entra.sh`; exported by hand with `scripts/azure/export-runtime-certificate.sh` to the server's compose secret file; never an App Configuration reference |

No storage secret exists: every storage account refuses Shared Key (`allowSharedKeyAccess: false`), so there is no account key, connection string or shared access signature to store, and the emulator's well-known development key is carried by the client library itself (ADR-0022).

## Keys (Key Vault)

| Key | Environment | Purpose |
|---|---|---|
| `Erp--Platform--DataProtection--Key` | local-dev, production | RSA 2048 key with `wrapKey`/`unwrapKey`; protects the ASP.NET Core Data Protection key ring from the `authentication` phase |
